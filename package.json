{
  "name": "michael-levin-archive-guide",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "python tools/generate_data.py",
    "test": "node --test tests/*.test.mjs",
    "check": "python tools/generate_data.py && node --test tests/*.test.mjs"
  }
}
